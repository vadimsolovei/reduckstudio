<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <link
      rel="preload"
      href="/assets/fonts/BebasNeue/BebasNeue-Bold.otf"
      as="font"
      type="font/otf"
      crossorigin
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or "Reduck Studio" }}</title>
    <link rel="stylesheet" href="/assets/styles.compiled.css" />
    <script>
      (function () {
        var theme = localStorage.getItem('theme');
        if (!theme) {
          theme = window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
        }
        document.write(
          '<link id="theme-stylesheet" rel="stylesheet" href="/assets/theme-' + theme + '.css" />'
        );
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>
  </head>
  <body>
    <div class="bg-decorator bg-decorator--1" aria-hidden="true"></div>
    <div class="bg-decorator bg-decorator--2" aria-hidden="true"></div>
    {% include "components/header.njk" %} {{ content | safe }} {% include "components/footer.njk" %}
    {% include "components/contact-form-modal.njk" %}

    <script src="/assets/script.js"></script>
  </body>
</html>
